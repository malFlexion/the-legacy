# Large raw data — download separately via scripts/... (508MB)
data/scryfall-cards.json

# card_index.pkl (44MB) IS committed — CI/Docker build needs it, and
# rebuilding it requires scryfall-cards.json (gitignored). Rebuild
# locally with `python src/card_index.py` if Scryfall data changes.

# vectordb/ is now committed — RAG needs it at runtime and it's only ~13MB.
# Rebuild locally with `python src/build_vectordb.py` if source docs change.

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Model weights (large files — download or rebuild)
*.bin
*.gguf
*.safetensors
*.pt
*.pth
merged-model/

# Exception: include the LoRA adapter (small, ~43MB)
!notebooks/lora-legacy/lora-adapter/adapter_model.safetensors

# Environment
.env
.env.local

# Test artifacts
.coverage
.pytest_cache/
htmlcov/
